import 'package:flutter/material.dart';

class SplaashScreen extends StatelessWidget {
  SplaashScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: CircularProgressIndicator()));
  }
}
